export enum RESOURCE {
  users = 'users',
}

export enum USER_ROLE {
  admin = 'ADMIN',
  superAdmin = 'SUPERADMIN',
  finance = 'FINANCE',
  editor = 'EDITOR',
  basic = 'BASIC',
}

export enum COLLECTIONS {
  users = 'users',
}
